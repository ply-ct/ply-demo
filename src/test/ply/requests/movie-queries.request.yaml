moviesQuery:
  url: '${baseUrl}/movies/year=${year}'
  method: GET
  headers:
    Accept: application/json

movieById:
  url: '${baseUrl}/movies/${id}'
  method: GET
  headers:
    Accept: application/json
